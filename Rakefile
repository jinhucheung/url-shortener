#!/usr/bin/env rake

require 'rubygems'
require 'sinatra'
require 'sequel'
require File.expand_path('../config/setting', __FILE__)

Dir[File.expand_path('../lib/tasks/**/*.rake', __FILE__)].each {|task| load task}